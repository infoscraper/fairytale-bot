[build]
builder = "nixpacks"

[deploy]
startCommand = "python init_db.py && python -m src.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[environments.production]
variables = { ENVIRONMENT = "production", DEBUG = "False" }
